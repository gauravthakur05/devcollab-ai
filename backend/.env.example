# Server
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# MongoDB
MONGODB_URI=MONGODB_URI=mongodb+srv://megaurav402815_db_user:me_gaurav_7@cluster0.4imffda.mongodb.net/devcollab?appName=Cluster0

# JWT
JWT_SECRET=replace_this_with_a_long_random_secret
JWT_EXPIRES_IN=7d

# AI provider (mock for now; swap to "openai" later without code changes elsewhere)
AI_PROVIDER=mock
AI_API_KEY=

# AWS (not required to run locally — reserved for future cloud deployment)
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=
